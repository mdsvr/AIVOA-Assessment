GROQ_API_KEY=your-groq-api-key-here
# Optional: separate key for the chat assistant, so it doesn't share a rate-limit
# bucket with extraction/classification/summary calls. Falls back to GROQ_API_KEY if unset.
GROQ_API_KEY_CHAT=
DATABASE_URL=postgresql+psycopg2://aivoa:aivoa@localhost:5432/aivoa
